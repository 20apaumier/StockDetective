export interface LineData {
    time: string;
    value: number;
}

export interface StockDataItem {
    date: string;
    open: number;
    high: number;
    low: number;
    close: number;
    volume: number;
}